{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Online Voting System",
            "type": "pwa-chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start PHP Server"
        }
    ]
}
